# GitDemo Repository

This project demonstrates the completed state of the Git hands-on exercises:
- Initial commit with `welcome.txt`
- `.gitignore` for filtering logs and temporary files
- Feature branching and conflict resolution in `hello.xml`
